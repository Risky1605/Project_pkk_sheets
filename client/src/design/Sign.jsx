const HeroSign = ({ icon }) => {
  return (
    <div className="size-[4rem] btn-joinUs rounded-full relative flex justify-center items-center">
      {icon}
    </div>
  );
};

export default HeroSign;
